# HuggingFace Space - Audio Quality Metrics API
# Runtime: Python 3.10+

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Audio processing
librosa==0.10.2.post1
soundfile==0.13.1
numpy==1.26.4
scipy==1.14.1
resampy>=0.4.3

# SRMR (Speech-to-Reverberation Modulation Ratio) - shimhz fork (matches TTS_Validation reference)
SRMRpy @ git+https://github.com/shimhz/SRMRpy.git

# SigMOS - uses local ONNX model (P.804 Microsoft SIG-Challenge)
onnxruntime==1.20.1

# VQScore - VQVAE model (requires PyTorch, einops, pyyaml)
pyyaml>=6.0
einops>=0.7.0

# WVMOS (wav2vec 2.0 MOS predictor)
wvmos @ git+https://github.com/AndreevP/wvmos.git

# UTMOS (MOS predictor via torch.hub - no separate package needed)

# PyTorch (CPU only, >=2.6 required for safe torch.load)
torch==2.6.0
torchaudio==2.6.0

# For model downloads at build time
requests>=2.31.0
